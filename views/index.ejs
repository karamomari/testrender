<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>

  <table class="table table-bordered border-primary">
    <thead class="table-dark">
      <tr>
        <th scope="col">#</th>
        <th scope="col">Title</th>
        <th scope="col">Body</th>
        <th scope="col">Number of like</th>
      </tr>
    </thead>
    <tbody>
        <% for (let index = 0; index < articel.length; index++) { %>
       <tr>
        <th scope="row"><%= articel[index].id.toString().substring(5, 10) %></th>

        <td><%= articel[index].title %></td>
        <td><%= articel[index].body %></td>
        <td><%= articel[index].numberOfLike %></td>
      </tr>
    <%       } %>
    </tbody>
  </table>
</body>
</html>